# Operator Monorepo Apps

- `apps/web` proxies the current frontend workflows from `Operator-web`.
- `apps/api` proxies the current API/runtime workflows from `Operator-web`.

This keeps behavior stable while shared packages are extracted.